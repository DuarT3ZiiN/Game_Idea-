#include "ReconciliationRuntime.h"

void ReconciliationRuntime::
ReceiveServerState(
    const AuthoritativeState& State
)
{
    // rollback

    // restore state

    // replay commands

    // rebuild prediction
}